{% extends 'base.html.twig' %}

{% block title %}Connexion{{ parent() }}{% endblock %}
{% block body %}
    {# <div class="container-log">
    {# bootstrap 
        {# <form class="toptop">
            <h2 class="">Connexion</h2>

            <div class="mb-3 mailsign">
                <p>Email :</p>
                <input type="email" class="form-control" placeholder="name@example.com">
            </div>
            <div class="mb-3 mailsign">
                <p>Mot De Passe :</p>
                <input type="password" class="form-control" placeholder="Password">
            </div>

            <div class="checkbox mb-3">
                <input type="checkbox" value="remember-me"> Se souvenir
            </div>
            <div class="mb-3 button-signin">
            <button class="button" type="submit">Connexion</button>
            </div>
        </form> 
    </div> #}


    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <form action="{{ path('user_app_login') }}" method="post" class="bg-white" style=" margin: 0 auto; padding-top:100px; text-align:center; ">
        
        <label for="username">Email:</label>
        <input type="text" id="username" name="_username" value="{{ last_username }}"/>

        <label for="password">Mot de Passe:</label>
        <input type="password" id="password" name="_password"/>

        <button type="submit">Connexion</button>
    </form>
{% endblock %}
